// type Person = {
//   name: string,
//   surname: string,
//   age: number
// }

// type Special = string | number | boolean;

// interface IPerson {
//   name: string;
//   surname: string;
//   age: number
// }

// interface IWorker  extends IPerson{
//   salary: number;
// }

// interface IBall {
//   color: string;
// }

// interface IBox {
//   balls: IBall[];
// }

function App() {
  // let box: IBox;

  // box = { balls: [{ color: "red" }] };

  // function sum(a: number, b: number, fun: (a: number) => string | number ) {
  //   // fun(a+b).replaceAll()
  //     fun(a+b).charAt()
  // }

  // function print( a: number): string{
  //   return a+'';
  // }

  // sum(1,3, print)

  // let arr = [1, 2, 3, 4];

  // let newArr: string[] = arr.map<string>((item) => item + "");

  return <div className="App">salam</div>;
}

export default App;
